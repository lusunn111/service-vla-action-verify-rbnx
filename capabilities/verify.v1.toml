[contract]
id = "robonix/service/vla/action_verify/verify"
version = "1"
kind = "service"
idl = "action_verify/srv/Verify.srv"
description = "Return a candidate VLA action using speculative inference with target-model fallback."

[mode]
type = "rpc"
